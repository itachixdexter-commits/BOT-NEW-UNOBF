[phases.setup]
nixPkgs = ["python3", "nodejs", "openjdk", "lua51", "lua52", "lua53", "lua54"]

[phases.install]
cmds = ["pip install -r requirements.txt", "npm install -g unluac-js"]

[start]
cmd = "python bot.py"
